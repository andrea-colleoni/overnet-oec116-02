server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri:  file:///${user.home}/Desktop/Temp/Corsi/OEC116/configs/
          searchPaths: '{application}/{profile}'
  