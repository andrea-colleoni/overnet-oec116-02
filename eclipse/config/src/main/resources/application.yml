server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri:  file:///var/config-server
          searchPaths: '{application}/{profile}'
  